// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as aws from '@pulumi/aws';
import * as pulumi from '@pulumi/pulumi';

export interface EventsRuleToEventsEventBusConnectorReadWriteArgs {
  source: aws.cloudwatch.EventRule;

  target: aws.cloudwatch.EventBus;

}

export class EventsRuleToEventsEventBusConnectorReadWrite extends pulumi.ComponentResource {
  constructor(name: string, args: EventsRuleToEventsEventBusConnectorReadWriteArgs, opts?: pulumi.ComponentResourceOptions) {
    super('aws-connectors:index:EventsRuleToEventsEventBusConnectorReadWrite', name, args, opts);
    new aws.iam.RolePolicy(`${name}-policy`, {
      role: args.source.roleArn.apply(arn => arn!),
      policy: {
        Version: '2012-10-17',
        Statement: [
          {
            Effect: 'Allow',
            Action: [
              'events:PutEvents',
            ],
            Resource: [
              pulumi.interpolate`${args.target.arn}`,
            ],
          },
        ],
      }
    });
    this.registerOutputs({});
  }
}