// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as aws from '@pulumi/aws';
import * as pulumi from '@pulumi/pulumi';

export interface StepFunctionsStateMachineToLambdaFunctionConnectorReadWriteArgs {
  source: aws.sfn.StateMachine;

  target: aws.lambda.Function;

}

export class StepFunctionsStateMachineToLambdaFunctionConnectorReadWrite extends pulumi.ComponentResource {
  constructor(name: string, args: StepFunctionsStateMachineToLambdaFunctionConnectorReadWriteArgs, opts?: pulumi.ComponentResourceOptions) {
    super('aws-connectors:index:StepFunctionsStateMachineToLambdaFunctionConnectorReadWrite', name, args, opts);
    new aws.iam.RolePolicy(`${name}-policy`, {
      role: args.source.roleArn,
      policy: {
        Version: '2012-10-17',
        Statement: [
          {
            Effect: 'Allow',
            Action: [
              'lambda:InvokeAsync',
              'lambda:InvokeFunction',
            ],
            Resource: [
              pulumi.interpolate`${args.target.arn}`,
            ],
          },
        ],
      }
    });
    this.registerOutputs({});
  }
}