// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as aws from '@pulumi/aws';
import * as pulumi from '@pulumi/pulumi';

export interface StepFunctionsStateMachineToSQSQueueConnectorReadWriteArgs {
  source: aws.sfn.StateMachine;

  target: aws.sqs.Queue;

}

export class StepFunctionsStateMachineToSQSQueueConnectorReadWrite extends pulumi.ComponentResource {
  constructor(name: string, args: StepFunctionsStateMachineToSQSQueueConnectorReadWriteArgs, opts?: pulumi.ComponentResourceOptions) {
    super('aws-connectors:index:StepFunctionsStateMachineToSQSQueueConnectorReadWrite', name, args, opts);
    new aws.iam.RolePolicy(`${name}-policy`, {
      role: args.source.roleArn,
      policy: {
        Version: '2012-10-17',
        Statement: [
          {
            Effect: 'Allow',
            Action: [
              'sqs:SendMessage',
            ],
            Resource: [
              pulumi.interpolate`${args.target.arn}`,
            ],
          },
        ],
      }
    });
    this.registerOutputs({});
  }
}