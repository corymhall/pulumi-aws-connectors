// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as aws from '@pulumi/aws';
import * as pulumi from '@pulumi/pulumi';

export interface LambdaFunctionToLambdaFunctionConnectorReadWriteArgs {
  source: aws.lambda.Function;

  target: aws.lambda.Function;

}

export class LambdaFunctionToLambdaFunctionConnectorReadWrite extends pulumi.ComponentResource {
  constructor(name: string, args: LambdaFunctionToLambdaFunctionConnectorReadWriteArgs, opts?: pulumi.ComponentResourceOptions) {
    super('aws-connectors:index:LambdaFunctionToLambdaFunctionConnectorReadWrite', name, args, opts);
    new aws.iam.RolePolicy(`${name}-policy`, {
      role: args.source.role,
      policy: {
        Version: '2012-10-17',
        Statement: [
          {
            Effect: 'Allow',
            Action: [
              'lambda:InvokeAsync',
              'lambda:InvokeFunction',
            ],
            Resource: [
              pulumi.interpolate`${args.target.arn}`,
            ],
          },
        ],
      }
    });
    this.registerOutputs({});
  }
}