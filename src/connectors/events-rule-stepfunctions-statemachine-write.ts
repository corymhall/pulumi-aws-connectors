// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as aws from '@pulumi/aws';
import * as pulumi from '@pulumi/pulumi';

export interface EventsRuleToStepFunctionsStateMachineConnectorReadWriteArgs {
  source: aws.cloudwatch.EventRule;

  target: aws.sfn.StateMachine;

}

export class EventsRuleToStepFunctionsStateMachineConnectorReadWrite extends pulumi.ComponentResource {
  constructor(name: string, args: EventsRuleToStepFunctionsStateMachineConnectorReadWriteArgs, opts?: pulumi.ComponentResourceOptions) {
    super('aws-connectors:index:EventsRuleToStepFunctionsStateMachineConnectorReadWrite', name, args, opts);
    new aws.iam.RolePolicy(`${name}-policy`, {
      role: args.source.roleArn.apply(arn => arn!),
      policy: {
        Version: '2012-10-17',
        Statement: [
          {
            Effect: 'Allow',
            Action: [
              'states:StartExecution',
            ],
            Resource: [
              pulumi.interpolate`${args.target.arn}`,
            ],
          },
        ],
      }
    });
    this.registerOutputs({});
  }
}