// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as aws from '@pulumi/aws';
import * as pulumi from '@pulumi/pulumi';

export interface SfnStateMachineToLambdaFunctionConnectorArgs {
  /**
   * The source resource.
   */
  source: aws.sfn.StateMachine;

  /**
   * The target resource.
   */
  target: aws.lambda.Function;
}

/**
 * Connect a StepFunctions StateMachine to a Lambda Function.
 */
export class SfnStateMachineToLambdaFunctionConnector extends pulumi.ComponentResource {
  constructor(name: string, args: SfnStateMachineToLambdaFunctionConnectorArgs, opts?: pulumi.ComponentResourceOptions) {
    super('aws-connectors:index:SfnStateMachineToLambdaFunctionConnector', name, args, opts);
    const statements: aws.iam.PolicyStatement[] = [];
    statements.push(
      {
        Effect: 'Allow',
        Action: [
          'lambda:InvokeAsync',
          'lambda:InvokeFunction',
        ],
        Resource: [
          pulumi.interpolate`${args.target.arn}`,
        ],
      },
    );
    new aws.iam.RolePolicy(`${name}-policy`, {
      role: args.source.roleArn,
      policy: {
        Version: '2012-10-17',
        Statement: statements,
      }
    }, opts);
    this.registerOutputs({});
  }
}