// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as aws from '@pulumi/aws';
import * as pulumi from '@pulumi/pulumi';

export interface AppSyncDataSourceToEventsEventBusConnectorArgs {
  /**
   * The source resource.
   */
  source: aws.appsync.DataSource;

  /**
   * The target resource.
   */
  target: aws.cloudwatch.EventBus;
}

/**
 * Connect a AppSync DataSource to a Events EventBus.
 */
export class AppSyncDataSourceToEventsEventBusConnector extends pulumi.ComponentResource {
  constructor(name: string, args: AppSyncDataSourceToEventsEventBusConnectorArgs, opts?: pulumi.ComponentResourceOptions) {
    super('aws-connectors:index:AppSyncDataSourceToEventsEventBusConnector', name, args, opts);
    const statements: aws.iam.PolicyStatement[] = [];
    statements.push(
      {
        Effect: 'Allow',
        Action: [
          'events:PutEvents',
        ],
        Resource: [
          pulumi.interpolate`${args.target.arn}`,
        ],
      },
    );
    new aws.iam.RolePolicy(`${name}-policy`, {
      role: args.source.serviceRoleArn.apply(arn => arn!),
      policy: {
        Version: '2012-10-17',
        Statement: statements,
      }
    }, opts);
    this.registerOutputs({});
  }
}