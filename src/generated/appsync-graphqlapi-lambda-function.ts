// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as aws from '@pulumi/aws';
import * as pulumi from '@pulumi/pulumi';

export interface AppSyncGraphQLApiToLambdaFunctionArgs {
  /**
   * The source resource.
   */
  source: aws.appsync.GraphQLApi;

  /**
   * The target resource.
   */
  target: aws.lambda.Function;
}

/**
 * Connect a AppSync GraphQLApi to a Lambda Function.
 */
export class AppSyncGraphQLApiToLambdaFunction extends pulumi.ComponentResource {
  constructor(name: string, args: AppSyncGraphQLApiToLambdaFunctionArgs, opts?: pulumi.ComponentResourceOptions) {
    super('aws-connectors:index:AppSyncGraphQLApiToLambdaFunction', name, args, opts);
    new aws.lambda.Permission(`${name}-policy`, {
      statementId: 'AppSyncGraphQLApiToLambdaFunction',
      action: 'lambda:InvokeFunction',
      function: args.target.name,
      principal: 'appsync.amazonaws.com',
      sourceArn: pulumi.interpolate`arn:${aws.getPartitionOutput({}, { parent: this }).partition}:appsync:${aws.getRegionOutput({}, { parent: this }).name}:${aws.getCallerIdentityOutput({}, { parent: this }).accountId}:apis/${args.source.id}`,
    }, { parent: this });
    this.registerOutputs({});
  }
}