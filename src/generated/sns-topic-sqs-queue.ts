// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as aws from '@pulumi/aws';
import * as pulumi from '@pulumi/pulumi';

export interface SNSTopicToSQSQueueArgs {
  /**
   * The source resource.
   */
  source: aws.sns.Topic;

  /**
   * The target resource.
   */
  target: aws.sqs.Queue;
}

/**
 * Connect a SNS Topic to a SQS Queue.
 */
export class SNSTopicToSQSQueue extends pulumi.ComponentResource {
  constructor(name: string, args: SNSTopicToSQSQueueArgs, opts?: pulumi.ComponentResourceOptions) {
    super('aws-connectors:index:SNSTopicToSQSQueue', name, args, opts);
    new aws.sqs.QueuePolicy(`${name}-policy`, {
      queueUrl: args.target.id,
      policy: {
        Version: '2012-10-17',
        Statement: [
          {
            Effect: 'Allow',
            Action: [
              'sqs:SendMessage',
            ],
            Resource: [
              pulumi.interpolate`${args.target.arn}`,
            ],
            Principal: {
              Service: 'sns.amazonaws.com',
            },
            Condition: {
              ArnEquals: {
                'aws:SourceArn': args.source.arn,
              },
            },
          },
        ],
      }
    }, { parent: this });
    this.registerOutputs({});
  }
}