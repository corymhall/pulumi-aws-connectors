// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as aws from '@pulumi/aws';
import * as pulumi from '@pulumi/pulumi';

export interface SNSTopicToLambdaFunctionArgs {
  /**
   * The source resource.
   */
  source: aws.sns.Topic;

  /**
   * The target resource.
   */
  target: aws.lambda.Function;
}

/**
 * Connect a SNS Topic to a Lambda Function.
 */
export class SNSTopicToLambdaFunction extends pulumi.ComponentResource {
  constructor(name: string, args: SNSTopicToLambdaFunctionArgs, opts?: pulumi.ComponentResourceOptions) {
    super('aws-connectors:index:SNSTopicToLambdaFunction', name, args, opts);
    new aws.lambda.Permission(`${name}-policy`, {
      statementId: 'SNSTopicToLambdaFunction',
      action: 'lambda:InvokeFunction',
      function: args.target.name,
      principal: 'sns.amazonaws.com',
      sourceArn: pulumi.interpolate`${args.source.arn}`,
    }, { parent: this });
    this.registerOutputs({});
  }
}